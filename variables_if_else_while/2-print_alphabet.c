#include <stdio.h>
/**
 *main - print the aplphabet
 *
 *Return: always return 0
 */
int main (void)
{
  char x = "%c";
    for (x = 'a'; x <= 'z'; ch++)
      {
      putchar(x);
      }
    return (0);
}
