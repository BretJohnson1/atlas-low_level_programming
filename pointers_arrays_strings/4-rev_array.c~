#include "main.h"
/**
 *reverse_array - reverse content
 *@a: checked output
 *@n: checked output
 *
 *Return: always return 0
 */
void reverse_array(int *a, int n)
{
	int i;

	for (i = 0; i <= n; I++)
	{
		if 
