#include "main.h"
/**
 *print_array - print array of integers
 *@a: checked output
 *@n: checked output
 *
 *Return: always return 0
 */
void print_array(int *a, int n)
{
  int len = 0;
  
  while (a != '\0')
    {
      printf("%s = ",  a)
	a++;
	}
  while (n <= len)
    {
      printf("%d");
    }
  -putchar('\n');
}
