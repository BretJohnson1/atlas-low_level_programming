#include "main.h"
/**
 *puts_half - print second half of string
 *@str: checked output
 *
 *Return: always return 0
 */
void puts_half(char *str)
{
  int len = 0;
  n = (len - 1 / 2)

    while ( len != '\0')
      {
	len++;
	for (n = len - 1 / 2)
	  {
	    _putchar(*str);
	      }
      }
  _putchar('\n');
