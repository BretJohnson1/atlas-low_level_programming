#include "main.h"
/**
 *reverse_array - reverse content
 *@a: checked output
 *@n: checked output
 *
 *Return: always return 0
 */
int reverse_array(int *a, int n)
{
	int i;
	int m;

	for (i = 0; i <= n; i++)
	{
		return (a[i]);

		for (m = n; m >= 0; m--)
		{
			return (a[i]);
		}
	}
}
