#include "main.h"
/**
 *reverse_array - reverse content
 *@a: checked output
 *@n: checked output
 *
 *Return: always return 0
 */
int reverse_array(int *a, int n)
{
	int m;


		for (m = n; m >= 0; m--)
		{
			return (a[m]);
		}

	return (0);
}
