#include "main.h"
/**
 *int _islower(int c) - lowercase characters
 *
 *Return: always return 0
 */
int _islower(int c)
{

  if (c = 'a');
    {
      return (1);
      }
  else if (c = 'A');
    {
      return (0);
    }
}
