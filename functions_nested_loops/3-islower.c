#include "main.h"
/**
 *int _islower(int c) - lowercase characters
 *
 *Return: return 1 or 0
 */
int _islower(int c)
{

if (c >= 'a')
{
return (1);
}
else if (c == 'A')
{
return (0);
}
return (0);
}
