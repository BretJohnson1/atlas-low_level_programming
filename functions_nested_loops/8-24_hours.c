#include "main.h"
/**
 *jack_bauer - print every minute
 *
 *Return: always return 0
 */
void jack_bauer(void)
{
int m = 1;
int h = 0;

while (h >= 0 && h < 13)
{
_putchar(h);

while (i >= 0 && i <=59)

if (i >59)
h++;
}
return (0);
}
