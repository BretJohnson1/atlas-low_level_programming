#include "main.h"
/**
 *print_last_digit - print last digit of number
 *@n: checked output
 *Return: last digit of number
 */
int print_last_digit(int n)
{
_putchar(n % 10);

return (n % 10);
}
return (0);
}
