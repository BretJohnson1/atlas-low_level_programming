#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 *main - add positive numbers
 *@argc: checked output
 *@argv: checked output
 *
 *Return: always return 0
 */
int main(int argc, char *argv[])
{
	int i;
	int result;

	while (scanf("%d", &i) ==1)
	{
		result += i
			}
		if (argc == 1)
		{
			printf("0\n");
		}
		else if (argv < 48 || argv > 57)
		{
			printf("Error\n");
			return (1);
		}
		return (0);
}
			
